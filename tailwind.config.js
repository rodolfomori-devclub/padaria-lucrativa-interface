export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  // In Tailwind v4, colors are defined in CSS using @theme directive
  // No need for theme.extend.colors anymore
  plugins: [],
} 